# Review: Positions tab — held stocks keep the general market header; position detail moves to a conditional "Positions" tab
Date: 2026-07-19
Status:

## Summary
Findings: 0 BLOCKERs, 1 ISSUE, 2 SUGGESTIONs, 2 QUESTIONs
Requires owner decision: PT-Q1 (Realized P/L no longer surfaced in the UI — confirm intended), PT-Q2 (four visual states across both routes — owner click-through)
Ready for Coding agent: PT-I1, PT-S1, PT-S2

Reviewed branch HEAD (`0aaf2272`) diffed against `origin/main` (`git diff origin/main...HEAD`). Working tree clean. The diff is scoped to the positions-tab feature only — no unrelated changes. `npm run verify` is green: typecheck ok, lint ok (only pre-existing warnings, none new), 99/99 tests, secret-scan clean.

The core correctness crux — the three-state panel gate — is **implemented correctly**. `getPositionsPanelState(position)` in `lib/utils/positions-tab.ts:39-44` gates on `position.quantity > 0` specifically (not `!!position`), and `components/research/transactions-tab.tsx:72-116` renders `"held"` → live stat band + table, `"closed"` → muted "Position closed." caption + table, `"none"` → "you do not hold" empty state + table. The `quantity>0` distinction the Designer flagged is enforced by a single shared, unit-tested helper, not duplicated inline. The legacy `components/transaction-history.tsx` is deleted and has zero remaining importers (grep confirmed — the only textual match is a comment in `transactions-tab.tsx`). The header swap on `/portfolio/[ticker]` reuses the already-fetched `quote` (no new query, `{quote && …}`-guarded, no null-deref) and the Buy more / Sell / Delete actions plus their modals are untouched (unchanged lines in the diff; the pre-existing `quantity > 0` gate on those actions/modals was not modified). The sell/FIFO flows were not disturbed.

## Findings

### PT-I1 — ISSUE
**File:** `components/research/transactions-tab.tsx:92-116`; `app/(dashboard)/portfolio/[ticker]/page.tsx` (removed header block, diff lines ~194-278); `DECISIONS.md` ADR-18 (Decision + Evidence)
**Problem:** Realized P/L is no longer displayed anywhere in the UI for a position that has sold shares. The old `/portfolio/[ticker]` header rendered a conditional "Realized P/L" cell (the removed `hasRealizedPL` block: `const hasRealizedPL = position.realizedPL !== undefined && position.realizedPL !== 0`). Both the plan (Task 2 / the header-relocation description) and ADR-18's Decision text explicitly list "Realized P/L" among the position-specific stats that "move out of the header into a tab." But the shared `TransactionsTab` stat band renders only **Shares held / Average cost / Market value / Unrealised P/L** — there is no Realized P/L cell, and `PositionResponse` in `transactions-tab.tsx:16-23` does not even carry a `realizedPL` field. For a fully-sold (`quantity === 0`) position the band is suppressed entirely, so realized gains are doubly hidden. Net effect: a user who partially or fully sold a position can no longer see their realized P/L in the app. This is a real (if low-severity) capability regression, and it makes ADR-18's Evidence/Decision claim ("Realized P/L … move into a tab") inaccurate — the number moves nowhere; it disappears.
**Recommendation:** Either (a) add a Realized P/L cell to the `TransactionsTab` "held" stat band (and decide whether to show it in the "closed" state, where it is arguably most relevant — a closed position's whole value is its realized result), extending `PositionResponse` to include `realizedPL` from the already-returned position payload; or (b) if dropping the UI surface for Realized P/L is an accepted scope reduction, correct ADR-18's Decision/Evidence text to say Realized P/L is *removed* rather than *relocated*, and note the gap in `TECH_DEBT.md`. Do not leave the ADR claiming a relocation that did not happen. This is a UI/display change only — it does not touch the realized-P/L computation or the sell/FIFO flow.

### PT-S1 — SUGGESTION
**File:** `app/(dashboard)/research/[symbol]/page.tsx:124-129`; `app/(dashboard)/portfolio/[ticker]/page.tsx:120-125`
**Problem:** The `activeTab` fallback is a post-render `useEffect` keyed on `[showPositionsTab]`. Because the effect fires after the render commit, there is a one-render window in which `activeTab === "transactions"` while the "transactions" entry has already been filtered out of `tabs`. In that window the tab body still renders (`{activeTab === "transactions" && <TransactionsTab …/>}`) even though no active tab button exists. In practice this is essentially unreachable: `activeTab` starts at `"overview"` on both routes, the tab only disappears when transactions go from present→empty (only via Delete on the portfolio route, which immediately `router.push("/dashboard")`), so the flagged transient never actually paints for a user. Correct as written, but the guard is defensive-only and relies on that timing.
**Recommendation:** Optional hardening — derive the effective active tab during render instead of correcting it after commit, e.g. `const effectiveTab = tabs.some(t => t.value === activeTab) ? activeTab : "overview"` and switch the body/`className` on `effectiveTab`. This removes the theoretical mismatch window entirely and drops the need for the `eslint-disable` on the effect. No behavioural change for real users.

### PT-S2 — SUGGESTION
**File:** `lib/utils/positions-tab.test.ts` (coverage scope)
**Problem:** The new pure helpers are well covered at the unit level (99/99 green; `shouldShowPositionsTab` happy/empty/null and `getPositionsPanelState` held/closed/none, including the `quantity: 0` crux). What is *not* covered anywhere is the piece of logic that actually consumes `getPositionsPanelState` — the three-way conditional render in `transactions-tab.tsx` (that `"closed"` shows the caption and still shows the table; that `"held"` shows the band). That is UI-level and there is no component-test / Playwright harness in this repo (consistent with prior reviews here), so it is acceptable to leave it to owner click-through rather than block on it — but it is worth stating explicitly that the caption rendering and the panel-suppression wiring are verified only by reading, not by test.
**Recommendation:** No action required for merge. If a component-test harness (e.g. `@testing-library/react` + vitest, or Playwright) is introduced later, add a rendering test that asserts: `quantity:0` → no stat band, "Position closed." caption present, transaction table still present; `quantity>0` → stat band present. Until then, PT-Q2 covers this by owner verification.

### PT-Q1 — QUESTION
**File:** `components/research/transactions-tab.tsx`; `DECISIONS.md` ADR-18
**Problem:** See PT-I1. This is surfaced as a QUESTION as well because whether the loss of the Realized P/L display is a defect or an accepted scope reduction is an owner call, not something the Coding agent can decide autonomously. The plan and ADR both implied relocation, not removal.
**Recommendation:** Owner to confirm: should Realized P/L be re-surfaced in the Positions tab (PT-I1 option a), or is dropping it from the UI accepted (PT-I1 option b, with the ADR text corrected)?

### PT-Q2 — QUESTION
**File:** `app/(dashboard)/portfolio/[ticker]/page.tsx`, `app/(dashboard)/research/[symbol]/page.tsx`, `components/research/transactions-tab.tsx`
**Problem:** The four visual states the feature turns on — (1) `/portfolio/[ticker]` held: general market grid populated + Buy more/Sell/Delete functional + Positions tab shows stat band + table; (2) `/research/[symbol]` held: unchanged header, 7th tab reads "Positions"; (3) `/research/[symbol]` never-transacted: 6 tabs, no Positions tab; (4) fully-sold (`quantity 0`): Positions tab shows table + "Position closed." caption, no zero-value band — plus base-currency reconversion in the panel — cannot be verified automatically (no Playwright project in this repo). The underlying logic *is* verified: the tab-visibility gate and the three-state panel gate are unit-tested pure helpers, and the component wiring is readable and correct. Judgement: the logic is verified; the live visual states are the residual.
**Recommendation:** Owner to click through the four states across both routes (and confirm the base-currency switch reconverts the stat band values). This mirrors NAV-Q1 / FVL-Q1 handling on prior work here — not a merge blocker on its own.

## Proposed DECISIONS.md entries
None. ADR-18 already exists and is accurate on the header swap, the tab rename/gating, the shared-component unification, and the `quantity>0` crux. The only ADR-18 correction needed is factual (the Realized P/L relocation claim) and is captured by PT-I1 — it should be fixed in the existing ADR text, not added as a new ADR.
