# DECISIONS.md

## ADR-1 — JWT session strategy over database sessions
- **Decision:** NextAuth with JWT sessions; `middleware.ts` authorises via `getToken()`, route handlers via `getServerSession()`.
- **Evidence:** `middleware.ts:6`, `lib/auth.ts:53-56`, `lib/utils/auth.ts:40`
- **Tradeoffs:** stateless and fast, no session table reads; but sessions cannot be revoked server-side, which raises the cost of the `NEXTAUTH_SECRET` exposure tracked in `TECH_DEBT.md` (leaked-secrets item).
- **Status:** accepted-but-flagged
- **Confidence:** High

## ADR-2 — API routes authorise individually; middleware excludes /api
- **Decision:** `middleware.ts` matcher excludes `/api`, so every route handler calls `getAuthenticatedUser()` / `getAuthenticatedUserWithPortfolio()` or an inline `getServerSession()` check itself.
- **Evidence:** `middleware.ts:21-23` (matcher), `lib/utils/auth.ts`
- **Tradeoffs:** per-route control and clean public/private mixing; but a forgotten guard silently ships an open endpoint. Five routes were found unauthenticated during the 2026-07-16 onboarding audit and have since been fixed (see `reviews/2026-07-16-onboarding.md` ONB-04/ONB-05). Two guard patterns now coexist (shared helper vs. inline `getServerSession`) — see `AGENT.md` known fragile surfaces.
- **Status:** accepted-but-flagged
- **Confidence:** High

## ADR-3 — Service layer between routes and Prisma
- **Decision:** business logic lives in `lib/services/*.service.ts`; route handlers stay thin and delegate.
- **Evidence:** `lib/services/` (9 services), `app/api/wishlist/[id]/route.ts:14` delegating to `wishlistService`
- **Tradeoffs:** testable and reusable logic; but ownership checks then live in the service (`wishlist.service.ts:113`), so a route that forgets to pass `userId` would bypass them.
- **Status:** accepted
- **Confidence:** High

## ADR-4 — In-process caching via node-cache and hand-rolled Maps
- **Decision:** no shared cache store; each service that caches does so independently — `node-cache` in `news.service.ts`, a separate `Map` in `market-data.service.ts`, another in `lib/middleware/rate-limit.ts`, plus 24h DB-backed caches on `FundamentalData`/`AnalystRating`.
- **Evidence:** `package.json` (`node-cache` dependency), `lib/services/market-data.service.ts`, `lib/services/news.service.ts`, `lib/middleware/rate-limit.ts:3`
- **Tradeoffs:** trivial to run locally, zero infra; but every cache is per-instance and lost on restart, won't be shared across serverless instances if deployed to a multi-instance target, and the rate limiter's effective limits are weaker than intended in that scenario.
- **Status:** accepted-but-flagged
- **Confidence:** Medium — inferred from usage; owner should confirm the deployment target before this is settled.

## ADR-5 — Closed positions are computed, not persisted
- **Decision:** there is no `ClosedPosition` model. `/api/portfolio/closed-positions` recomputes win/loss outcomes on every request via FIFO matching of `Transaction` rows.
- **Evidence:** `prisma/schema.prisma` (no `ClosedPosition` model), `app/api/portfolio/closed-positions/route.ts:27`
- **Tradeoffs:** no migration/backfill risk, always consistent with transaction history; but recomputes from scratch on every request with no caching, so cost grows with transaction history size, and the FIFO logic exists in exactly one place with no test coverage as of 2026-07-16.
- **Status:** accepted-but-flagged
- **Confidence:** High

## ADR-6 — Dev and production share one database
- **Decision:** dev and production currently point at the same Supabase database. `.env`/`.env.local` hold two connection strings for it, used simultaneously for different purposes: `DATABASE_URL` (transaction-mode pooler, port 6543) for the running app, `DIRECT_URL` (session-mode pooler, port 5432) for `prisma migrate`/`prisma db push`. (An earlier version of this entry described the two strings as selected based on which network the developer is on — that described the pre-reconnect Supabase project, not the current one; corrected 2026-07-17 per AUD-01.)
- **Evidence:** owner confirmation, 2026-07-16 (see `reviews/2026-07-16-onboarding.md` ONB-11); `.env`, `.env.local`, `.env.example`, `prisma/schema.prisma:7,10`
- **Tradeoffs:** simplest possible setup for a single-developer project; but any dev-side mutation (manual testing, a bad migration, a seed script) affects real user data with no isolation, and there is no way to test schema changes safely before they hit production.
- **Status:** accepted-but-flagged
- **Confidence:** High

## ADR-7 — Accept the leaked NEWS_API_KEY while non-production
- **Decision:** `NEWS_API_KEY` remains live and publicly readable in git history (commits `2a6c4c1a`, `3855042e`, in both `.env` and `.env.local`). The owner accepts this risk **conditional on this app not being in production**. `NEXTAUTH_SECRET`, `GEMINI_API_KEY`, and `DATABASE_URL` were rotated on 2026-07-17; this key was not, because newsapi.org's free tier exposes no regenerate or revoke control.
- **Evidence:** `lib/services/news.service.ts` (sole consumer); `TECH_DEBT.md` TD-01; `reviews/2026-07-16-onboarding.md` ONB-01. Leak verified by hashing the current `.env` against `3855042e:.env` — that check also confirms the other three are rotated.
- **Tradeoffs:** Anyone reading the public repo can spend this repo's NewsAPI quota, and the key cannot be revoked, so the exposure runs until a fresh key replaces it. Accepted because the blast radius is confined to a free-tier news quota: the key grants no access to our data, our users, or any account of value. This is explicitly *not* the reasoning that applied to `NEXTAUTH_SECRET`, which could forge sessions for any user and was rotated on that basis.
- **Condition (this is what makes the decision valid):** the app is not deployed. Going to production changes the calculus — a live app makes quota exhaustion a user-visible outage rather than a private annoyance. TD-01 therefore carries a blocking precondition: obtain a fresh NewsAPI key before any production deploy.
- **Supersedes:** the 2026-07-16 acceptance of ONB-01, which was made on the incorrect belief that the repo was a local-only clone. This one is made with the public exposure confirmed.
- **Status:** accepted-but-flagged
- **Confidence:** High (on the reasoning; the condition is the thing to watch — a decision conditional on "we won't deploy" silently expires the moment someone deploys)

## ADR-8 — Meridian tokens replace the shadcn HSL-triple values in place; drop the `hsl()` wrapper
- **Decision:** one token system, not two. Meridian's palette is written directly into the existing shadcn variable names in `app/globals.css` (`--background`, `--card`, `--border`, `--foreground`, …), with Meridian-only tokens (`--ink`, `--sub`, `--mut`, `--line2`, `--fill`, `--up`, `--dn`, `--amber`, `--btnbg`, `--btnfg`) added alongside. The `hsl(var(--x))` wrapper is removed from `tailwind.config.js` so variables hold complete colors (hex/oklch) rather than bare HSL triples. The theme switch stays on `darkMode: ["class"]` / `.dark` — the design's `data-theme="dark"` is a prototype artifact, and swapping the mechanism changes nothing visually while costing a custom Tailwind variant.
- **Rationale (both facts verified against this codebase, 2026-07-17):** (1) `hsl()` cannot parse hex or oklch — `hsl(#faf8f4)` and `hsl(oklch(...))` are invalid CSS, so keeping the wrapper would force lossy HSL conversion of exactly the three oklch accents (`--up`/`--dn`/`--amber`) that carry gain/loss meaning. (2) The wrapper exists in exactly one file: `grep -rE 'hsl\(var\(--' components/ui/*.tsx` returns **nothing** — all 20 UI components consume Tailwind aliases (`bg-card`, `border-border`) that `tailwind.config.js` resolves. So one file's change reskins all 20 components, Radix primitives included, with zero component edits.
- **Evidence:** implemented — `tailwind.config.js` (colors, `hsl()` wrapper removed from every entry except the unused `--chart-*` set), `app/globals.css:6-84` (`:root`/`.dark` blocks with the full Meridian token set). Proof of the "zero component edits" claim: `git diff --stat components/ui/` against `origin/main` is empty.
- **Tradeoffs:** the Tailwind color config diverges from the stock shadcn idiom, so future `npx shadcn add` output will arrive expecting `hsl(var(--x))` and needs its color values adjusted (mitigated by an `AGENT.md` fragile-surface entry). Opacity modifiers (`bg-card/50`) still work — Tailwind emits `color-mix()` for non-HSL vars. Rejected alternatives: a parallel token set (two palettes to sync forever; guarantees drift), and converting Meridian to HSL triples (lossy on the oklch accents; permanently blocks the design's color space).
- **Status:** accepted
- **Confidence:** High — the two load-bearing facts were verified empirically rather than recalled.

## ADR-9 — "Watchlist" is user-visible copy only; code and schema stay `wishlist`
- **Decision:** the Meridian design's "Watchlist" is adopted in UI copy (nav label, H1, buttons). The route (`/wishlist`), component files, `wishlist.service.ts`, the API routes, and the Prisma `Wishlist`/`WishlistItem` models keep the `wishlist` name.
- **Evidence:** implemented — `components/navigation.tsx` (nav label "Watchlist"), `app/(dashboard)/wishlist/page.tsx` (H1 "Watchlist", "+ Add to watchlist" button), `components/wishlist-table.tsx` (dialog/toast copy). Route (`/wishlist`), `wishlist.service.ts`, `app/api/wishlist/**`, and the Prisma `Wishlist`/`WishlistItem` models are unchanged (grep confirms no renamed files or routes).
- **Tradeoffs:** copy and code disagree, which is a real papercut for anyone navigating the codebase from the UI. Accepted because the alternative — renaming models — is a migration against a database that dev and prod **share** (ADR-6): real risk of data loss, zero visual payoff, and squarely outside a re-skin's scope. "Watchlist" is also the more accurate domain term (and the term `PRODUCT.md` already uses in prose), so the copy change is a correction, not a rebrand. Logged in `TECH_DEBT.md` for a future dedicated rename.
- **Status:** accepted
- **Confidence:** High

## ADR-10 — Purpose-built inline SVG for the dashboard chart; Recharts retained elsewhere
- **Decision:** `components/portfolio-chart.tsx` is rewritten as an inline SVG area chart using a ported Catmull-Rom→bezier path builder (`lib/utils/chart-path.ts`) plus a 500ms eased `requestAnimationFrame` morph on range change. `components/price-chart.tsx` (research detail) keeps Recharts and is retokenized only.
- **Correction of record:** `ARCHITECTURE.md` listed Tremor as the charting library. It is a dependency with **zero imports** — `grep -rn "@tremor/react" app components` returns nothing. Both charts are and were Recharts. `ARCHITECTURE.md` is corrected and the unused dependency logged as debt.
- **Rationale:** the design is declared high-fidelity, and its two headline chart behaviors are precisely what Recharts does not do — `type="monotone"` is monotone-cubic interpolation (deliberately overshoot-suppressing), not Catmull-Rom; and Recharts re-renders on data change rather than interpolating between two series. The prototype already contains a complete ~10-line implementation of both. The custom SVG is *less* code than the Recharts usage it replaces.
- **Evidence:** implemented — `lib/utils/chart-path.ts` (`buildPath`/`buildAreaPath`, pure), `lib/utils/chart-path.test.ts` (9 tests: known input, single/two-point degenerate, flat series, NaN/Infinity input), `components/portfolio-chart.tsx` (inline SVG chart with `requestAnimationFrame` eased morph on range change, 10 ranges retained: 1D/1W/1M/3M/6M/YTD/1Y/5Y/10Y/FROM_START). `components/price-chart.tsx` keeps Recharts, retokenized only (ink line via `var(--foreground)`, `var(--line2)` gridlines).
- **Tradeoffs:** gives up Recharts' tooltips/axis autoscaling on the dashboard chart, and hand-rolled SVG means we own the edge cases (degenerate/flat/empty series) — mitigated by unit-testing `buildPath` directly, which is cheaper and higher-signal than any test of the Recharts version would have been. Confined to one component deliberately: `price-chart.tsx` keeps Recharts because its axes and tooltips are worth more there than pixel-parity.
- **Status:** accepted
- **Confidence:** Medium — the path math is verified against the prototype and unit-tested; "matches the design pixel-for-pixel" is confirmable only by eye.

## ADR-12 — Intrinsic-value route returns 200-with-null for the "no fundamental data" sentinel, not 500

- **Decision:** `app/api/research/[symbol]/intrinsic-value/route.ts` catches the specific `Error("No fundamental data available")` thrown by `IntrinsicValueService.calculateIntrinsicValue` and returns **200** with a well-formed "unavailable" payload (`intrinsicValue: null, upside: null, upsidePercent: null, methods: [], confidence: "low"`) instead of falling through to the generic 500 handler. All other errors still return 500.
- **Evidence:** `app/api/research/[symbol]/intrinsic-value/route.ts` (catch block), `lib/services/intrinsic-value.service.ts:36` (the sentinel throw, unchanged), `components/intrinsic-value.tsx` (migrated to React Query; renders the `HeadlineScoreCard` shell with em-dash placeholders whenever `res.ok`, reserving the full-card `AlertCircle` failure for a genuine network/parse error).
- **Rationale:** "no `FundamentalData` cached yet for this symbol" is an expected data-absence condition — thin market-data coverage (e.g. ENGI.PA), or simply that the Fundamental tab (the only writer of `FundamentalData`) hasn't been opened yet for this symbol in this session (cold cache) — not a server error. The previous 500 made the component's `!res.ok` check indistinguishable from a real failure, so it always rendered the full-card `AlertCircle` state even on well-covered symbols like AAPL when Intrinsic value was opened first. This is a status-code bug fix (an expected condition was reported as a server error), not a new endpoint or a scoring-logic change — the calculation itself is untouched.
- **Tradeoffs:** callers of this route that assumed 500 meant "no data" (none found — the route has one known consumer, `components/intrinsic-value.tsx`, plus `wishlist.service.ts`'s direct service call which was never routed through HTTP, see the wishlist fragile-surface entry in `AGENT.md`) need to check `methods.length === 0` / `intrinsicValue === null` on a 200 response instead of catching an error. Rejected alternative: fix entirely at the component boundary by inspecting the 500 body's error string — rejected because matching on error-message text across a fetch boundary is more fragile than a status-code contract, and the plan explicitly permitted this narrow backend fix.
- **Status:** accepted
- **Confidence:** High — the sentinel string and its one throw site were verified directly in `intrinsic-value.service.ts`.

## ADR-11 — Custom Meridian SVG chart on the research detail tabs; retire Recharts there
- **Decision:** the research-detail Overview and Technical charts are rendered by a new `components/research/detail-price-chart.tsx` reusing `lib/utils/chart-path.ts` (`buildPath`/`buildAreaPath`, unchanged), with hover crosshair+tooltip, a minimal set of y-axis price labels (`niceYTicks`), and optional dashed reference lines as props. `components/price-chart.tsx` (Recharts) is retired from the detail flow and deleted once no importer remains. This scopes-amends ADR-10, which kept Recharts on the detail chart.
- **Evidence:** implemented — `components/research/detail-price-chart.tsx` (`DetailPriceChart`, reuses `buildPath`/`buildAreaPath` unchanged), `lib/utils/chart-ticks.ts` (`niceYTicks`, unit-tested), `components/overview.tsx` (period="1Y"), `components/technical-analysis.tsx` (period="6M"), `app/(dashboard)/research/[symbol]/page.tsx` (7-tab wiring). `components/price-chart.tsx` (Recharts) deleted 2026-07-18 after confirming no remaining importer. Playwright interaction pass confirmed hover crosshair+tooltip (date + price at cursor), 3 y-axis labels, and dark-mode token inversion — see `plans/2026-07-18-meridian-research-detail.md` Verification.
- **Tradeoffs:** gives up Recharts' built-in axis autoscaling and tooltip on the detail charts, so the hover interaction and y-axis ticks are hand-rolled (mitigated by extracting `niceYTicks` and the crosshair math as tested primitives). Gains pixel-parity with the dashboard hero chart (owner requirement) and satisfies the owner's hover-tooltip and y-axis-label requirements with one component the hero can later adopt. ADR-10's original "keep Recharts for axes/tooltips" reasoning was about avoiding rebuild cost — the owner has now chosen to pay that cost for parity, so this is a scoped amendment, not a contradiction. Reference lines/legend (Technical) are not yet fed real support/resistance values — see `TECH_DEBT.md` TD-DTL-SR.
- **Status:** accepted
- **Confidence:** Medium — the path math and viewBox parity are verified against the prototype; the hover pixel-mapping under `preserveAspectRatio="none"` was confirmed via a Playwright interaction pass (container-ref approach, screenshot-verified tooltip).

## ADR-13 — Per-query staleTime replaces the global 30s refetch default; portfolio liveness stays on usePriceSync
- **Decision:** the `QueryClient` in `components/providers.tsx` no longer sets `refetchInterval` as a global default — every query in the app previously re-hit its (often external-API-backed) endpoint every 30s regardless of staleness. `refetchOnWindowFocus: false` also becomes the default. Individual queries that need periodic freshness (e.g. `wishlist/page.tsx`'s `refetchInterval: 60000`, `portfolio/[ticker]/page.tsx`'s quote poll, `news-feed.tsx`'s 5-min poll) keep their own explicit `refetchInterval` — those are deliberate per-call-site choices, unaffected by this change. Research/reference-data queries without an explicit policy (`technical-analysis.tsx`, `detail-price-chart.tsx`, the research-detail ownership query) get an explicit `staleTime: 5*60*1000`.
- **Evidence:** implemented — `components/providers.tsx` (defaultOptions), `components/technical-analysis.tsx:35-42`, `components/research/detail-price-chart.tsx:52-59`, `app/(dashboard)/research/[symbol]/page.tsx:64-73`. `plans/2026-07-18-performance-audit-remediation.md` Task 1.
- **Rationale:** portfolio price liveness is already handled by `usePriceSync` (an explicit 5-min sync + invalidate) — a 30s poll on *every* query was redundant with that one deliberate refresh path and was the single largest source of background load in the app (every open research tab, the wishlist, and the dashboard silently re-hit expensive external-API-backed endpoints every 30s). Verified via Playwright: idling 65s on a research-detail page fires zero repeat `/api/market|news|research` requests post-change.
- **Tradeoffs:** research/wishlist data is no longer auto-live between navigations — a stale research tab left open won't silently refresh until the user re-navigates or its own explicit poll (where one exists) fires. Accepted because this data is reference/analysis material, not a live ticker tape; if the owner later wants live-updating research data, the newly-explicit `staleTime` values are the knob to adjust.
- **Status:** accepted
- **Confidence:** High — verified by network-level Playwright observation, not just code inspection.

## ADR-14 — Composite Transaction index created and applied, with owner sign-off
- **Decision:** `prisma/schema.prisma`'s `Transaction` model gains `@@index([positionId, executedAt])` — the access pattern for the closed-positions FIFO replay (ADR-5). The migration (`prisma/migrations/20260718152229_add_transaction_position_executedat_index`) was generated via `prisma migrate dev --create-only`, held pending owner sign-off, and has now been **applied** to the shared dev/prod database via `prisma migrate deploy`, with owner approval (2026-07-18).
- **Evidence:** `prisma/schema.prisma:86-89`, `prisma/migrations/20260718152229_add_transaction_position_executedat_index/migration.sql` (single additive `CREATE INDEX`, no destructive statements). `prisma migrate status` confirms the migration is now applied — "Database schema is up to date!", no pending migrations. `plans/2026-07-18-performance-audit-remediation.md` Task 6; `reviews/2026-07-18-performance-audit-remediation.md` PAR-Q1 (owner sign-off).
- **Rationale:** dev and production share one database (ADR-6/TD-02) — there is no isolated environment to apply and test a migration against before it hits real data. The plan's Open decisions section required explicit owner sign-off before `prisma migrate deploy` runs; the owner gave that sign-off on 2026-07-18. A `@@index([type])` addition (also mentioned in the plan as conditional on query-plan benefit) was not pursued — no isolated DB exists to safely run `EXPLAIN` against, so that decision remains deferred rather than guessed at.
- **Tradeoffs:** the closed-positions hot path (ADR-5: "recomputes from scratch on every request... cost grows with transaction history size") now benefits from the index. Applying a schema migration to a shared dev/prod database always carries some risk with no isolated environment to test against first — accepted here because the migration is a single additive `CREATE INDEX` with no destructive statements, and the owner explicitly signed off.
- **Status:** accepted
- **Confidence:** High (the SQL itself is safe and additive, and application is now confirmed via `prisma migrate status`).

## ADR-16 — Page-route auth is enforced only in middleware; the dashboard layout does not re-check the session
- **Decision:** `app/(dashboard)/layout.tsx` no longer calls `getServerSession` + `redirect`. Page-route authentication is enforced solely by `middleware.ts` (`getToken()` → redirect to `/login`), which runs before the layout on every matched route. The layout is a synchronous Server Component that renders the `<Navigation>` + `<main>` shell with no server round-trip in its render path.
- **Evidence:** implemented — `app/(dashboard)/layout.tsx` (no `next-auth`/`redirect`/`authOptions` import; synchronous function component returning `<div><Navigation /><main>{children}</main></div>` directly), `middleware.ts:5-23` (the sole surviving page-route gate, unchanged by this change). Manually re-verified 2026-07-19: unauthenticated `curl` requests to `/dashboard`, `/wishlist`, `/portfolio/closed-positions`, `/research/AAPL` all still return `307` → `/login`, confirming the middleware (not the removed layout check) was always what gated these.
- **Tradeoffs:** removes a defence-in-depth second check — if the middleware matcher were ever narrowed to exclude a dashboard route, that route would become unauthenticated with no layout-level backstop. Accepted because (a) the middleware is already the documented single source of truth for page-route auth (ADR-2), (b) the layout's `await getServerSession()` blocked the entire dashboard subtree — including the shared `<Navigation>` and every route's `loading.tsx` Suspense fallback — from painting until it resolved, which was the primary cause of the reported navigation freeze, and (c) any future defence-in-depth check here must be non-blocking (Suspense-wrapped), not a render-gating `await`.
- **Status:** accepted
- **Confidence:** High — the middleware-covers-all-dashboard-routes fact is verifiable from the matcher (`middleware.ts:22` excludes only `api`/`_next`/`favicon`), the layout guard was strictly redundant with it, and Next.js's own documented component rendering hierarchy (`layout → template → error → loading (Suspense boundary) → page`) confirms a layout with no blocking render-path `await` cannot delay a child segment's `loading.tsx` fallback — the mechanism this fix relies on.

## ADR-15 — Resilient `quoteSummary` wrapper: catch Yahoo schema-validation drift, warn, use the coerced result
- **Decision:** all `yahoo-finance2` `quoteSummary` calls go through a shared `safeQuoteSummary(symbol, queryOptions)` in `lib/yahoo-finance.ts`. It keeps the library's Zod validation ON, catches the throw by `error.name === "FailedYahooValidationError"` (not `instanceof` — the v3 package neither exposes an `errors` subpath in its `exports` map nor an `errors` property on the instance, and deep-importing the error class would violate the exports map / break bundling), logs a `console.warn` with the drifted field paths, and returns the library's already-coerced `error.result`. If `error.result` is nullish it re-throws; all non-validation errors re-throw unchanged. Call sites keep their own critical-module guards (`market-data`/`fundamentals` throw when the structurally-required module is absent; `analyst-ratings` does not, since a missing `recommendationTrend` is a valid "no coverage" state).
- **Evidence:** `lib/yahoo-finance.ts` (`safeQuoteSummary`), `lib/services/fundamental-analysis.service.ts:90-99` (call + `price`/`summaryDetail` guard), `lib/services/market-data.service.ts:52-59` (call + existing `!quote.price` guard), `lib/services/analyst-ratings.service.ts:50-57` (call, no added guard), `lib/yahoo-finance.test.ts`, `lib/services/fundamental-analysis.service.test.ts`, `lib/services/market-data.service.test.ts`, `lib/services/analyst-ratings.service.test.ts`.
- **Tradeoffs:** the wrapper's return type is untyped (`Record<string, any>`), pushing shape-guarding to callers — accepted because the three extractors already treat the response as `Record<string, any>` with heavy null-guarding, so the burden already lived there. We keep validation on (vs. blanket `validateResult: false`) specifically to retain the drift signal; the cost is a `try/catch` on every call and a warn log on each drift, which is the intended observability. Rejected: (a) blanket `validateResult: false` — silences the signal AGENT.md's no-silent-wrong-number rule depends on; (b) per-call-site patching — leaves a fourth future call site free to regress.
- **Status:** accepted
- **Confidence:** High — the library's v3 error shape, the missing `errors` export, and the `error.name`/`error.result` fields were verified directly against the installed `yahoo-finance2@3.13.0`, and all four unit test files pass under mocks with no live Yahoo dependency.

## ADR-17 — Dev server runs Turbopack (`next dev --turbopack`); production build untouched
- **Decision:** the `dev` script in `package.json` is `next dev --turbopack` — the local dev server uses Next.js 15's stable Turbopack bundler instead of the default webpack dev bundler. `build` (`next build`) and `start` (`next start`) are unchanged and continue to use Next's default production bundler. This is a dev-tooling change only; no source code, config, or production behavior changes.
- **Evidence:** implemented — `package.json:6` (`"dev": "next dev --turbopack"`; `build`/`start` unchanged, confirmed by `git diff package.json` showing exactly one changed line). Verified booting 2026-07-19: ready banner read `▲ Next.js 15.5.4 (Turbopack)` / `✓ Ready in 1189ms`; `/dashboard`, `/wishlist`, `/portfolio/closed-positions` each compiled and served 200 with no bundler/loader/PostCSS error (`✓ Compiled /dashboard in 647ms`, `✓ Compiled /wishlist in 252ms`, `✓ Compiled /portfolio/closed-positions in 258ms`); the served stylesheet chunk returned 200 `text/css` with the expected Meridian tokens (`--background: #faf8f4` light / `#171411` dark) present, confirming Tailwind/PostCSS compiled cleanly under Turbopack. `next.config.js` verified vanilla (no `webpack()` fn, no custom loaders/plugins/aliases; only `serverExternalPackages`/`eslint.dirs`, both bundler-agnostic), `postcss.config.js` standard `tailwindcss`+`autoprefixer` (Turbopack-native), Tailwind v3.3 plugin loaded via `require()` in `tailwind.config.js` (bundler-agnostic), no SVG-as-component imports or custom loaders in source (grep, 2026-07-19).
- **Rationale:** the reported first-visit navigation freeze (whole screen frozen with no skeleton on the first click to a route, warm on every repeat) is Next.js dev-mode on-demand per-route compilation under plain `next dev` — the route's JS, including its `loading.tsx`, doesn't exist until that first compile finishes, so nothing paints. This is a dev-only artifact (absent in production, where `next build` pre-compiles all routes and `<Link>` prefetches). Turbopack is the recommended, GA dev bundler in Next 15 and compiles on-demand routes dramatically faster (~1s → ~100–200ms typical), shrinking the freeze below perceptibility without changing the app. Distinct from ADR-16, which removed the layout's blocking `getServerSession()` await that froze *warm* navigations too — that fix is already in; this addresses the residual first-visit-only compile gap.
- **Tradeoffs:** Turbopack does not honor a custom `webpack()` config the way webpack does — none exists here, so nothing is lost, but a future `webpack()` addition to `next.config.js` would silently not apply to the dev server and would need a Turbopack (`turbopack`/`experimental.turbo`) equivalent (noted in `AGENT.md`). Dev and prod now use different bundlers, a minor parity gap accepted because prod correctness is gated by `next build` in CI, not the dev server. Rejected: leaving dev on webpack (the freeze persists); a heavier fix (e.g. eager route pre-compilation) — unnecessary given a supported one-flag change resolves it.
- **Status:** accepted
- **Confidence:** High — the vanilla-`next.config.js` and no-custom-loader facts were grep-verified against this codebase (2026-07-19), `next dev --turbopack` is the documented stable dev flag for the installed Next.js 15.5.4, and the clean-compile/correct-styling claims were verified live against a running dev server (see Evidence), not just inferred. The perceived-freeze-eliminated claim itself remains owner-verifiable (Task 3, `plans/2026-07-19-meridian-first-visit-latency.md`) — no automated Playwright check exists in this repo to measure it directly; the compile-time numbers above are corroborating, not conclusive, evidence for that specific claim.

## ADR-18 — Held stocks keep the general market header; position detail moves into a conditional "Positions" tab, on one shared tab component
- **Decision:** the stock detail views stop showing a position-centric header for held stocks. `app/(dashboard)/portfolio/[ticker]/page.tsx`'s header switches to the same general market grid (Current price / Day range / 52-week range / Market cap) that `app/(dashboard)/research/[symbol]/page.tsx` already renders, fed by the quote both pages already fetch. The position-specific stats (Market value / Unrealized P/L / Avg cost / Shares) move out of the header into a tab **renamed from "Transactions" to "Positions"**, shown **only when transactions exist for the symbol** ("has or had a position"). Both routes render that tab body through the single shared `components/research/transactions-tab.tsx`, retiring the legacy `components/transaction-history.tsx` shadcn `Card` table. The two routes are **retained** (not consolidated) — `/portfolio/[ticker]` keeps Buy more / Sell / Delete actions the research view lacks; consolidating them is deferred (Option A, logged in `future_ideas.md`). OD-1 was resolved by the owner as Option B (keep both routes, align behaviour) before implementation began. **Realized P/L is re-surfaced in the Positions tab, for both the held and closed states** (owner decision PT-Q1, `reviews/2026-07-19-positions-tab.md`, PT-I1) — a settled historical figure valid regardless of current quantity, unlike Market value / Unrealised P/L which are meaningless at zero shares. Getting a real per-position figure required a follow-on fix beyond relocation: `GET /api/portfolio/positions/[ticker]` had hardcoded `realizedPL: 0` (stale comment claiming the field wasn't in the schema) because `Position` genuinely has no persisted per-position `realizedPL` column — only `Portfolio.realizedPL`, a portfolio-wide accumulator, exists (`sell/route.ts`). The route now computes it on read via the same FIFO service `GET /api/portfolio/closed-positions` already uses, applied to one position's own transaction history.
- **Evidence:** implemented — `lib/utils/positions-tab.ts` (`shouldShowPositionsTab`, `getPositionsPanelState`, `hasRealizedPL` — all unit-tested in `lib/utils/positions-tab.test.ts`); `lib/services/realized-pl.service.ts` (`computePositionRealizedPL`, directly unit-tested in `realized-pl.service.test.ts`'s `computePositionRealizedPL` `describe` block — no-sells, single-lot partial sell gain/loss, full-sell close, multi-lot FIFO ordering across one sell and across two cumulative sells, buy/sell fee proration, and the unmatched-remainder `avgCostBasis` fallback; also covered indirectly end-to-end via `app/api/portfolio/positions/[ticker]/route.test.ts`'s held/closed/no-sells/404 cases) — sums realized P/L across a position's SELL transactions, FIFO-matched against its own BUY transactions, mirroring the closed-positions route's per-position algorithm; `app/api/portfolio/positions/[ticker]/route.ts` (calls `computePositionRealizedPL` instead of hardcoding `realizedPL: 0`, unit-tested in `route.test.ts` for held/closed/no-sells/404 cases); `app/(dashboard)/research/[symbol]/page.tsx` (tab relabeled "Positions", transactions query + `tabs` filtered via `shouldShowPositionsTab`, `effectiveTab` derived at render time so a disappearing tab never leaves a one-render mismatch window — PT-S1); `app/(dashboard)/portfolio/[ticker]/page.tsx` (same relabel/filter/render-time `effectiveTab`, general market quote grid, spec-identical to `research/[symbol]/page.tsx`, fed by the `quote` this page already fetched pre-change; `TransactionsTab` replaces the deleted `TransactionHistory`); `components/research/transactions-tab.tsx` (three-state render — `getPositionsPanelState` gates on `quantity > 0` specifically, not `!!position`, per the crux correctness rule: `"held"` shows the live stat band — a 5th Realized P/L cell added when `hasRealizedPL`, otherwise the band stays 4-col — `"closed"` shows the muted italic "Position closed." caption plus the Realized P/L figure when `hasRealizedPL`, `"none"` keeps the pre-existing "you do not hold" empty state); `components/transaction-history.tsx` deleted (grep confirmed zero remaining importers after the `portfolio/[ticker]` swap).
- **Tradeoffs:** held stocks regain always-visible general market context and the two detail routes converge on one position-detail component (less drift). Cost: `/portfolio/[ticker]` and `/research/[symbol]` still duplicate header/tab scaffolding (the deeper consolidation, ADR-worthy in its own right, is deferred — logged in `future_ideas.md`). Position actions staying in the header is a deliberate scope boundary that keeps the fragile sell/FIFO flows untouched. Realized P/L is computed on every read (no caching), matching the existing closed-positions route's own no-caching precedent — acceptable at current data volumes, same tradeoff already accepted there. Rejected: redirect `/portfolio/[ticker]` → `/research/[symbol]` (Option A) — cleaner but migrates the mutation actions and touches dashboard link targets, larger blast radius than the owner's stated ask. Rejected: adding a persisted `Position.realizedPL` column — a schema migration was out of scope for a display-layer fix and the closed-positions route already establishes on-read computation as the accepted pattern for this exact number.
- **Status:** accepted
- **Confidence:** High — implemented and verified: `npm run verify` green (typecheck, lint with no new warnings, 106/106 tests including the realized-P/L route tests and the closed-position edge cases, secret-scan clean); the quantity>0-vs-position-exists distinction (the crux correctness risk the Designer flagged) is enforced by a single shared, tested helper rather than duplicated inline logic; the realized-P/L figure is verified against the same tested FIFO algorithm the closed-positions route already uses, not a new/independent calculation.

## ADR-19 — Analyst revisions + target Low/High persisted on `AnalystRating` via an additive migration, owner-signed-off
- **Decision:** `AnalystRating` gains three columns — `targetLowPrice Decimal?`, `targetHighPrice Decimal?`, `revisions Json?` — populated in `saveToDatabase` (from `extractRatings`' output, revisions already windowed by the new `filterRecentRevisions` helper) and read back in `formatCachedData` on a 24h cache hit, closing the gap TD-DTL-REV2 tracked (`formatCachedData` previously hardcoded these three to `null`/`[]` because no DB column existed, so a warm cache row for a heavily-covered symbol like NVDA always rendered the "no revisions" empty state even though a fresh fetch genuinely had data — 982 upstream `upgradeDowngradeHistory` entries for NVDA, 32 within 90 days, verified via a live probe). This resolves OD-1 of `plans/2026-07-20-analyst-revisions-nvda-fix.md` as **Option A**, mirroring ADR-14's owner-signed-off precedent for this exact codebase. Shipped together with a second, independent bug fix: no 90-day window filter previously existed anywhere, so a cold fetch would have returned all 982 raw entries mislabelled under the UI's "last 90 days" copy — `filterRecentRevisions(revisions, now, windowDays = 90, cap = 25)` is a new pure helper (window inclusive of the boundary instant, excludes future-dated entries, sorts newest-first, caps to 25) applied inside `extractRatings` before persistence.
- **Evidence:** `prisma/schema.prisma` (`AnalystRating.targetLowPrice`/`targetHighPrice`/`revisions`); `prisma/migrations/20260719231015_analyst_revisions_low_high/migration.sql` (single additive `ALTER TABLE "AnalystRating" ADD COLUMN ...` statement, no destructive statements — generated via `prisma migrate dev --create-only`, confirmed **not applied** by `prisma migrate status`); `lib/services/analyst-ratings.service.ts` (`filterRecentRevisions`, `saveToDatabase` populates all three columns, `formatCachedData` reads them back with `Array.isArray` / falsy-coalescing guards for rows written before the migration applies); `lib/services/analyst-ratings.service.test.ts` (`filterRecentRevisions` describe block — window/boundary-inclusive/boundary-exclusive/future-dated/cap/custom-params cases; updated cache-hit test asserting persisted values are returned; new back-compat test asserting a row with absent revisions/low/high columns still yields `[]`/`null`).
- **Tradeoffs:** a schema migration on the shared dev/prod database (ADR-6) — there is no isolated environment to apply and test against first; mitigated because the SQL is a single additive `ADD COLUMN` (no destructive statements) and the owner has explicitly signed off on this exact class of change before (ADR-14). `revisions` is stored as a JSON blob rather than a normalized child table — chosen because it is a small (≤25 item), display-only, bounded list with no query/filter/join requirement of its own; a normalized table would add migration/join complexity for no functional benefit at this scale. **The migration is created but NOT yet applied** — it requires an explicit owner `prisma migrate deploy` (or equivalent sign-off) before the new columns exist in the live database; until then, `saveToDatabase` writes against a Prisma Client that has been locally regenerated (`prisma generate`) to know about the new columns, but those writes will fail against the actual (unmigrated) table until deploy happens.
- **Status:** accepted-but-pending-deploy — the code and migration file are both merged/mergeable, but the migration must be applied by the owner before this fix is live in either environment (shared dev/prod DB, ADR-6).
- **Confidence:** High — the root cause was verified against live Yahoo data (not guessed): a probe confirmed NVDA has 982 `upgradeDowngradeHistory` entries (32 within 90 days) on a fresh fetch, and the only path returning `[]` was the cache-hit `formatCachedData` gap this ADR closes. The migration SQL is a single additive statement, consistent with ADR-14's already-verified-safe pattern. `npm run verify` passes locally with the regenerated Prisma Client; the "pending deploy" caveat is a process/sequencing fact, not a correctness uncertainty.

## ADR-20 — Per-user scoring weights in a dedicated table of discrete `Float?` columns; store raw, normalize at scoring time
> **Superseded in part by ADR-22 (2026-07-21):** the store-raw + auto-normalize + live-normalized-% UX below is replaced by **direct whole percentages validated to sum to 100 per group**. The dedicated-table / discrete-`Float?`-columns / normalize-at-scoring-time structure and the byte-identical-defaults guarantee are unchanged — only the input model and the stored number's scale change. Read ADR-22 for the current model.
- **Decision:** a new `UserScoringPreferences` model (1:1 with `User`, `userId @unique`, `onDelete: Cascade`) stores each user's custom scoring weights as **ten discrete nullable `Float` columns** — five composite category weights (`wCompositeIntrinsic`/`wCompositeFundamental`/`wCompositeTechnical`/`wCompositeSentiment`/`wCompositeAnalyst`) and five fundamental subcategory weights (`wFundValuation`/`wFundProfitability`/`wFundGrowth`/`wFundFinancial`/`wFundDividend`), **not** a single `Json` blob. Weights are stored **raw** (exactly what the user typed — arbitrary non-negative relative numbers) and **normalized to sum 1.0 per group at scoring/read time**, never at write time, via the pure `normalizeWeights()` in `lib/utils/scoring-weights.ts`. Defaults live once in `DEFAULT_SCORING_WEIGHTS` (same file) and equal the current hardcoded values exactly — composite from `components/overview.tsx` (pre-change), fundamental from `lib/services/fundamental-analysis.service.ts` (pre-change) — so a user with no row (or null columns) scores byte-identically to today. Edge cases (documented + tested in `normalizeWeights`): negatives clamp to 0; an all-zero group falls back to **defaults** (a curated distribution the user has seen), not equal weights; a single non-zero weight yields 1.0/0.0.
- **Evidence:** implemented — `prisma/schema.prisma` (`UserScoringPreferences` model + `User.scoringPreferences` back-relation), `prisma/migrations/20260720220007_user_scoring_preferences/migration.sql` (additive-only: `CREATE TABLE`, two `CREATE INDEX`, one `ADD CONSTRAINT` FK — no `DROP` statements; generated via `prisma migrate dev --create-only`, **applied** to the shared dev/prod database 2026-07-21, confirmed by `prisma migrate status` — "Database schema is up to date!"), `lib/utils/scoring-weights.ts` (`DEFAULT_SCORING_WEIGHTS`, `normalizeWeights`/`normalizeCompositeWeights`/`normalizeFundamentalWeights`, `weightedFundamentalTotal`, `weightedCompositeTotal`, `weightsEqualDefaults`; `lib/utils/scoring-weights.test.ts`, 22 tests covering defaults-normalize-to-themselves, all-zero→defaults, single-nonzero→1.0, negative-clamping, and default-weighted totals reproducing the pre-change numbers), `lib/services/scoring-preferences.service.ts` (`getWeights`/`saveWeights`, coalescing null columns and a missing row to defaults; `lib/services/scoring-preferences.service.test.ts`, 8 tests, Prisma mocked), `app/api/settings/scoring-weights/route.ts` (GET/PUT; `route.test.ts`, 6 tests).
- **Tradeoffs:** discrete columns over Json — the JSON-simplicity argument (ADR-19's `revisions`) does not apply because the set is fixed and known at schema-design time (exactly ten scalars; a sixth composite dimension would be an ADR-level scoring-model change, not a preferences-shape change), so typed self-documenting columns with DB defaults win. `Float` over `Decimal` — these are relative UI weights, not money; no exact-decimal need, matching `FundamentalData`'s numeric columns. Store-raw over store-normalized — keeps the settings round-trip lossless (UI shows back exactly what was typed) and confines all normalization to one tested pure function rather than splitting it across write and read. All-zero→defaults over →equal-weights — equal weights would silently change a user's scores to a distribution they never chose. Cost: another owner-gated migration against the shared dev/prod DB (ADR-6) — mitigated because it is purely additive (`CREATE TABLE`/`CREATE INDEX`/FK, no destructive statements), the same class ADR-14/ADR-19 already signed off. **The migration has been applied** (2026-07-21, `prisma migrate status` confirms "Database schema is up to date!") — `UserScoringPreferences` exists in the live (shared dev/prod) database, and `GET`/`PUT /api/settings/scoring-weights` and the per-user reweight paths are live against the real table.
- **Status:** accepted (owner approved the plan 2026-07-20) — **migration applied 2026-07-21** (see ADR-22's Status line for the confirming `prisma migrate status` check).
- **Confidence:** High — the default values were read directly from the two cited source locations (not recalled), both groups sum to 1.0 as authored, so the backward-compat guarantee is arithmetic, not aspirational, and it is now also asserted by 22 passing unit tests.

## ADR-21 — Composite score reweighted client-side, fundamental score reweighted server-side; the shared fundamental cache stays user-independent
- **Decision:** the two configurable scores apply the user's weights in different places, matching where each score is already computed. (1) **Composite — client-side.** `components/overview.tsx` fetches the user's category weights once (`GET /api/settings/scoring-weights`, a `useQuery`, `staleTime: Infinity`) and recomputes the composite in a `useMemo` over the five dimension scores + normalized weights, so changing a weight (or the query resolving) re-derives the composite instantly with no market-data refetch. (2) **Fundamental — server-side, weights passed as a pure parameter.** `fetchFundamentals(symbol, fundamentalWeights?)` gains an optional weights parameter; the route (`app/api/market/fundamentals/[symbol]/route.ts`) reads the authenticated user's prefs and passes them in — the service **never** reads `UserScoringPreferences` itself (routes do I/O, services stay pure — ADR-3). The reweight is a pure post-step (`applyPerUserReweight`, calling `weightedFundamentalTotal(breakdown, normalizeFundamentalWeights(weights))`) applied to the weight-independent `score.breakdown` carried through **both** the fresh path and the 24h cache path (`formatCachedData` reads `scoreDetails.breakdown`). **Crucially, the per-user reweighted total is applied on read and never written back:** `FundamentalData.fundamentalScore`/`scoreDetails` keep storing the default-weighted total + the weight-independent breakdown (persisted via `saveToDatabase` *before* the reweight step runs), so the symbol-keyed shared cache remains user-independent (ADR-4 preserved) — only `score.total` is re-derived per user, from the same breakdown every user shares. `lib/services/wishlist.service.ts` (Task 8) consumes the identical shared functions — `getWishlistWithScores` loads the user's weights once per call and passes `weights.fundamental` to `fetchFundamentals`, replacing its own former hardcoded composite weights + inline formula with `weightedCompositeTotal(scores, normalizeCompositeWeights(weights.composite))` — so there is exactly one definition of both formulas in the codebase (verified by grep: the only literal weight objects remaining are inside `DEFAULT_SCORING_WEIGHTS`).
- **Evidence:** implemented — `components/overview.tsx` (`weightsQ` query, `compositeWeights`/`composite` `useMemo`s, `isLoading` OR-gate extended to include `weightsQ.isLoading`, `hasCustomWeights` driving the "Your weighting · updated daily" meta-kicker swap), `components/fundamental-analysis.tsx` (same `["scoring-weights"]` query + `hasCustomWeights` comparison driving the "Your weighting · trailing twelve months" meta-kicker swap), `lib/services/fundamental-analysis.service.ts` (`fetchFundamentals(symbol, fundamentalWeights?)`, `applyPerUserReweight` private method applied to both the cache-hit and fresh-fetch return paths; `calculateFundamentalScore`'s own default-weighted total now also calls `weightedFundamentalTotal(breakdown, DEFAULT_SCORING_WEIGHTS.fundamental)` instead of a second inline weights object + formula), `app/api/market/fundamentals/[symbol]/route.ts` (migrated to `getAuthenticatedUser()`, loads `getWeights(auth.userId)`, passes `weights.fundamental`), `lib/services/wishlist.service.ts` (`getWishlistWithScores` calls `getWeights(userId)` once, passes `weights.fundamental` to `fetchFundamentals(item.ticker, weights.fundamental)`, and `weightedCompositeTotal(scores, normalizedCompositeWeights)` replaces the former inline composite formula). Tests: `lib/services/fundamental-analysis.service.weights.test.ts` (7 tests — fresh + cache paths, no-weights byte-identical, custom-weights total differs with breakdown unchanged, cache row never rewritten with a per-user total), `app/api/market/fundamentals/[symbol]/route.test.ts` (4 tests), `lib/services/wishlist.service.weights.test.ts` (4 tests, including the wishlist-equals-overview single-definition invariant and a grep-based test asserting no hardcoded weights object remains outside `lib/utils/scoring-weights.ts`).
- **Tradeoffs:** the split means the composite math lives in two languages of intent (a React `useMemo` and, for the fundamental subscore, a server call) — mitigated by extracting **all** weighted-sum + normalization math into one pure shared module (`lib/utils/scoring-weights.ts`) that the client composite, the server fundamental, and the wishlist all import, so there is a single tested implementation of each formula (confirmed by grep, not just intent). Applying the fundamental reweight on read (per request, per user) rather than caching it costs a trivial pure recompute over five numbers — far cheaper than the alternative (a per-user cache row), and it is what keeps the shared cache clean. Rejected: making the service DB-aware (fetch prefs inside `fetchFundamentals`) — violates ADR-3 and would couple the pure calc service to the auth/session layer; rejected: recomputing the composite server-side and refetching on every weight change — defeats the instant-feedback requirement and adds a round-trip per slider move. Cost carried over from ADR-20 is resolved: the `20260720220007_user_scoring_preferences` migration was applied 2026-07-21 (see ADR-22's Status line), so GET/PUT and the per-user reweight paths are live against the real table.
- **Status:** accepted (owner approved the plan 2026-07-20) — migration applied 2026-07-21, feature live.
- **Confidence:** High — the weight-independence of `score.breakdown` (the fact that makes the on-read reweight correct and cache-safe) was verified by reading both the fresh (`calculateFundamentalScore`) and cache (`formatCachedData`) paths directly; both carry the per-subcategory breakdown scores independent of the weight block. The single-definition invariant is verified by an automated grep-based test, not just code review.

## ADR-22 — Scoring weights are direct whole percentages validated to sum to 100 (supersedes ADR-20's auto-normalize UX)
- **Decision:** the scoring-weights UX changes from **store-raw-relative-numbers + auto-normalize + live-normalized-% readout** (ADR-20/OD-4) to **direct whole percentages (0–100) that the user makes sum to exactly 100 per group** (owner request, 2026-07-21, `plans/2026-07-21-scoring-weights-direct-percent.md`). The percent a user types **is** the weight — no silent normalization at the UI. The Settings page validates each group's running total and **disables Save until the group sums to 100** within an epsilon of 0.01 (guarding FP dust; whole percents sum cleanly). Values are constrained to `[0,100]`. The old per-column live-normalized-% ruled band is removed and replaced by a single live group-total/validity indicator ("Total: X% / 100%"). **The underlying scoring math is unchanged and needs no change** because it is *scale-invariant*: `normalizeWeights` divides each weight by its group sum, so a group stored as percentages `{25,25,20,15,15}` normalizes to the identical fractions `{0.25,…}` and produces byte-identical scores. `DEFAULT_SCORING_WEIGHTS` stays as fractions (the canonical scoring scale); conversion to/from whole percents happens at exactly one boundary — the settings API/service — via three new pure helpers in `lib/utils/scoring-weights.ts`: `fractionsToPercents` (×100 with largest-remainder rounding repair so the group sums to exactly 100), `percentsToFractions` (÷100), and `sumsTo100(weights, epsilon=0.01)` (validation predicate). The three scoring consumers (`overview.tsx`, `fundamental-analysis.service.ts`, `wishlist.service.ts`) are **unchanged** — they already pipe stored weights through `normalizeCompositeWeights`/`normalizeFundamentalWeights`, which absorb the scale change. **Existing/legacy rows** (raw relative numbers written under ADR-20) are presented on GET as their normalized-to-100 percentages via `getWeightsForSettings` (normalize→`fractionsToPercents`), so a pre-existing user sees a valid 100-sum split with unchanged scores; no data migration and no new schema migration — the same generic `Float` columns now carry percents, only the number's meaning/scale changes in code.
- **Evidence:** implemented — `lib/utils/scoring-weights.ts` (`fractionsToPercents`/`percentsToFractions`/`sumsTo100`, generic pure helpers, `normalizeWeights`/`weightedCompositeTotal`/`weightedFundamentalTotal`/`weightsEqualDefaults`/`DEFAULT_SCORING_WEIGHTS` untouched), `lib/utils/scoring-weights.test.ts` (new `fractionsToPercents`/`percentsToFractions`/`sumsTo100` describe blocks plus a `scale-invariance regression (ADR-22 guard)` block asserting `weightedCompositeTotal`/`weightedFundamentalTotal` are byte-identical for a percent-scaled group vs. its fraction-scaled equivalent, and that an arbitrary legacy raw row scores identically to its `fractionsToPercents(normalize(...))` presentation — 51 tests total in this file, all passing), `lib/services/scoring-preferences.service.ts` (`getWeightsForSettings(userId)` — loads via the unchanged `getWeights`, normalizes, converts to percents; `saveWeights` reworked to require all five keys present, each a finite number in `[0,100]`, and `sumsTo100(group)`, throwing `InvalidScoringWeightsError` otherwise — the old partial-merge/negative-only validation is replaced, not extended), `lib/services/scoring-preferences.service.test.ts` (`getWeightsForSettings` cases for missing-row/legacy-raw-row/already-percent-row; `saveWeights` cases for sum=94, sum=101, out-of-range value, missing key, non-finite value, valid 100-sum accept), `app/api/settings/scoring-weights/route.ts` (GET calls `getWeightsForSettings`; PUT unchanged in shape, still maps `InvalidScoringWeightsError`→400), `app/api/settings/scoring-weights/route.test.ts` (GET-returns-100-sum-percents, PUT-94-returns-400, PUT-valid-100-returns-200 cases), `app/(dashboard)/settings/page.tsx` (direct-percent inputs with a "%" label suffix per dimension, the live group-total/validity status line replacing the removed normalized-% band — `--mut` when valid, `--dn` when not, matching DESIGN.md exactly — Save disabled until `sumsTo100(group) && dirty`, Reset sets `fractionsToPercents(DEFAULT_SCORING_WEIGHTS...)`), `app/(dashboard)/settings/loading.tsx` (the 5-col `SkeletonStatBand` replaced with a single `kicker`-variant block per the revised skeleton spec), `lib/utils/scoring-weights-settings-gate.ts` (new pure module — `computeGroupTotalState`/`toNumbers` — extracted because Next.js App Router `page.tsx` files may only export `default`/reserved route-config names, so the total/validity/save-gate logic could not live as a named export inside `page.tsx` itself; this also gives it a React-free unit-test seam since `@testing-library/react` is not installed in this repo), `lib/utils/scoring-weights-settings-gate.test.ts` (Save-disabled-at-94/101, Save-enabled-at-100-and-dirty, Save-disabled-at-100-when-pristine, running-total-reflects-inputs, Reset-yields-100-sum-and-dirty cases — the Task 6 fallback since no component-test seam exists).
- **Tradeoffs:** direct percentages over auto-normalize — clearer for the user (the number typed is the weight; no type-and-watch-normalized loop the owner disliked) at the cost of requiring the user to make the group sum to 100 themselves (mitigated by the live total indicator + Reset-to-defaults). Keeping the internal scoring scale as fractions over switching everything to percents — leaves `normalizeWeights`/the two weighted-total functions/the three consumers and ADR-20's byte-identical-defaults assertion provably untouched, confining the change to a single conversion boundary; the cost is one extra accessor (`getWeightsForSettings`) alongside the unchanged `getWeights`. `fractionsToPercents` largest-remainder repair over naive rounding — guarantees legacy raw rows present as an exactly-100 split (naive rounding can yield 99/101). No data migration for the barely-used table (applied 2026-07-21, still low-row-count in practice) — normalizing-on-read into percents is cleaner and handles both new and legacy rows uniformly. Single-source-of-truth (one scoring definition in `lib/utils/scoring-weights.ts`) is preserved — the new helpers are conversion/validation, not a second weights definition. Extracting the settings page's total/validity gate into its own pure module (`scoring-weights-settings-gate.ts`) rather than inlining it in `page.tsx` was forced by Next.js's route-module export constraint, not a preference — accepted because it also gave the logic a cleaner, React-free test seam.
- **Status:** accepted (implemented 2026-07-21) — the ADR-20 table structure and defaults guarantee remain accepted; the `20260720220007_user_scoring_preferences` migration (created under ADR-20, unchanged by this plan) was **applied** to the shared dev/prod database on 2026-07-21 (`prisma migrate status` confirms "Database schema is up to date!") — the feature is live.
- **Confidence:** High — scale-invariance of `normalizeWeights` is arithmetic (verified against the implemented function: it divides by the group sum), so the "scoring unchanged" guarantee is provable, not aspirational, and is now also asserted by a dedicated scale-invariance regression test block; the defaults ×100 were checked to sum to exactly 100 (composite 25/25/20/15/15; fundamental 30/30/20/15/5) both by hand and by a passing unit test. `npm run verify` passes in full (typecheck, lint with no new warnings, all tests including the new ADR-22 coverage, secret-scan).

## ADR-23 — Named investment-style presets are whole-percent settings-layer data; applying one is a client-side form populate, no new API
- **Decision:** add `SCORING_STYLE_PRESETS` (with a `ScoringStylePreset` type and a `presetsForGroup(group)` helper) to `lib/utils/scoring-weights.ts`, beside `DEFAULT_SCORING_WEIGHTS` — six composite / five fundamental named allocations (Value, Quality/GARP, Growth, Momentum [composite only], Dividend/Income, Balanced), stored as **whole percentages summing to 100 per group** (the same scale ADR-22 established for the settings API/UI boundary), NOT fractions and NOT the internal scoring scale. Applying a preset on `app/(dashboard)/settings/page.tsx` is a **purely client-side populate** of the existing per-section input state (identical mechanism to the existing "Reset to house defaults"): `setInputs(toInputs(preset[group]))`. It issues no `PUT`, adds no API path, no service function, and no schema/migration — the user reviews the populated values and presses the existing Save, which flows through the unchanged `saveWeights` (every preset group already sums to 100, so it passes `sumsTo100` and the existing dirty+valid Save gate with no special casing). The **Balanced** preset is derived from `fractionsToPercents(DEFAULT_SCORING_WEIGHTS.*)`, not re-typed as literals, so the house default has one source of truth. No "active preset" state is stored or displayed — presets are a starting point, not a saved mode.
- **Evidence:** not-implemented — planned in `plans/2026-07-21-scoring-style-presets.md`. On implementation: `lib/utils/scoring-weights.ts` (`ScoringStylePreset`, `SCORING_STYLE_PRESETS`, `presetsForGroup`), `lib/utils/scoring-weights.test.ts` (preset block — every group sums to 100, expected keys/coverage per preset, `balanced` deep-equals `fractionsToPercents(DEFAULT_SCORING_WEIGHTS.*)`), `app/(dashboard)/settings/page.tsx` (per-section preset picker calling `setInputs(toInputs(preset[group]))`).
- **Tradeoffs:** whole-percent preset data over fractions — matches the settings-layer scale so applying a preset needs no `fractionsToPercents` conversion at the call site, keeping ADR-22's single fractions↔percents boundary intact; presets never touch the three scoring consumers, which stay fraction-based. Client-side populate over a new "apply preset" API — lower risk, preserves the explicit-save discipline the whole feature rests on (ADR-22 rejected auto-save), and reuses the existing PUT/`saveWeights` path a preset's percents are already valid for. Deriving Balanced from `DEFAULT_SCORING_WEIGHTS` over hand-typing it — one source of truth for the house default; a future default change flows into Balanced automatically (locked by a unit test). Rejected: storing which preset is active (adds schema/state for no functional need — a preset is indistinguishable from a hand-typed equal allocation once saved, by design); a blend/merge semantic (a named allocation must replace, not merge). The Dividend/Income composite numbers are the owner's stated values, flagged tunable — a one-line edit later with no structural impact.
- **Status:** proposed
- **Confidence:** High — all eleven preset groups were verified to sum to exactly 100; the client-side-populate path reuses the exact `setInputs(toInputs(...))` mechanism the existing Reset button already uses, and the existing Save gate accepts any 100-sum group without change.
