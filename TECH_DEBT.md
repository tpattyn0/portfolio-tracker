# TECH_DEBT.md

## Backlog

| ID | Severity | Impact | Effort | Recommended fix |
|----|----------|--------|--------|-----------------|
| TD-37 | Low | No centralized environment-variable validation — the two dead modules that purported to provide it (`lib/env.ts`, `lib/utils/env.ts`) were deleted 2026-07-23 (TD-09); current behavior is raw `process.env` reads with per-call-site guards. | Medium | Needs a fail-loud-vs-degrade decision per variable and a real import point (this app has no single server-bootstrap module every request transits — see `plans/2026-07-23-lib-cleanup-batch.md`'s TD-09 rationale) before a replacement is worth building. |
| TD-39 | Low | Neither `research/[symbol]/page.tsx` nor `portfolio/[ticker]/page.tsx` normalizes the route param's case before using it, so `/research/aapl` and `/portfolio/AAPL` produce `["quote","aapl"]` vs `["quote","AAPL"]` — two cache entries, silently splitting the cross-page cache hit the shared `["quote", symbol]` key (ADR-28) was built for. Consequence today is a duplicate fetch, never wrong data — all in-app navigation is generated from stored/uppercase tickers, so this doesn't misbehave in practice (`reviews/2026-07-23-td08-td15-cleanup.md` TD815-S2). **Investigated 2026-07-23 and deliberately not fixed in that session:** on both pages the same route-param variable (`symbol`/`ticker`) also drives page display text (the ticker kicker, name fallback), outbound links (`/portfolio/add?ticker=...`, `AddToWishlistModal`), and is prop-drilled as `symbol=`/`ticker=` into 6+ child tab components, plus two more page-local (non-shared) query keys/fetch URLs (`position`, `transactions`/`news`) per page — normalizing only the shared `quote` query's key would introduce a second, narrower-normalized variable used inconsistently with the rest of the same page, which is its own partial-application risk. | Low | **Both-pages-or-neither constraint, recorded explicitly:** if ever fixed, derive one canonical uppercase symbol once per page and use it consistently for **every** query key, fetch URL, prop, link, and display value on **both** `research/[symbol]/page.tsx` and `portfolio/[ticker]/page.tsx` — not just the shared `quote` query. A one-sided or partial normalization (e.g. only the `quote` queryKey) is worse than none: it guarantees the exact split it would be meant to prevent, on top of a new per-page inconsistency between which query is normalized and which isn't. |
| TD-01 | Medium | **Partially resolved 2026-07-17.** `NEXTAUTH_SECRET`, `GEMINI_API_KEY` and `DATABASE_URL` have been rotated and the leaked values are now dead (verified by hashing the current `.env` against `3855042e:.env`). `NEWS_API_KEY` **is still live and still public** in commits `2a6c4c1a` / `3855042e` — the owner cannot regenerate it (newsapi.org free tier exposes no rotate/revoke control) and has **accepted the risk on the explicit condition that this app is not in production** (2026-07-17). Exposure while non-production: anyone can spend this repo's NewsAPI quota; there is no access to our data or users via this key. Used by `lib/services/news.service.ts`. All three keys also remain readable in history regardless of rotation — rotation kills the values, it does not unpublish them. | Low | **Blocking condition — do not deploy to production while this is open.** Before any production deploy: obtain a fresh NewsAPI key (new account if the free tier still offers no rotation, or a paid tier that does), retire the old one, and close this item. Optional afterwards: make the repo private, and/or scrub history with `git filter-repo` — neither is a substitute for a fresh key. |
| TD-28 | High | The `secret-history` CI job is `continue-on-error: true`, so it reports the TD-01 leak on every run but cannot block a merge. This is deliberate — TD-01 is a known, owner-accepted debt (ADR-7) and gating on it would red-wall every unrelated PR — but it means "no secrets in history" is currently an aspiration, not a guarantee. | Low | When TD-01 is fully resolved (fresh NewsAPI key + history scrubbed, or repo made private with the dead findings suppressed by fingerprint in a committed history-scoped ignore file), delete `continue-on-error` from `.github/workflows/verify.yml` and add `secret-history` as a required status check alongside `code-gate` in the branch ruleset. The job comment carries the same instruction. |
| TD-02 | High | Dev and production share one Supabase database (confirmed by owner 2026-07-16, see ADR-6). Any dev-side mutation affects real user data with no isolation. **Deferred to launch by owner decision 2026-07-23** — a full plan exists at `plans/2026-07-23-td-02-dev-prod-db-split.md` (status `deferred`) and is ready to resume as-written. Rationale: there is no deployed environment (verified — no `.vercel`, no deploy platform config, no hosting reference in any doc), so "production" is the owner's local app run against the database holding his real portfolio; the only actor who can damage it is the owner, deliberately. At launch a production database must be provisioned anyway with different requirements (paid tier, backups, connection limits, possibly a different region), so splitting now would mean picking a topology to be revisited within months. Note the free-tier cost concern that prompted the deferral does **not** hold on its own — Supabase free tier allows two active projects, so a dev project costs a project slot, not money; the deferral rests on the launch-timing argument, not on cost. | Medium | **Trigger: launch preparation, before any production deploy.** Resume `plans/2026-07-23-td-02-dev-prod-db-split.md` — the existing database becomes production, a new empty project becomes dev (no production data ever copied), plus the `lib/utils/db-environment.ts` guard + `predev` hook so the split is enforced mechanically rather than by discipline. The guard was deliberately *not* taken early: with a single database the only ref present is the production ref, so it would block every `npm run dev` and require a standing opt-out flag that hollows it out. Its value arrives with the second database. |
| TD-03 | High | `next` has a critical RCE/DoS/cache-poisoning vulnerability per `npm audit` (many CVEs across RSC flight protocol, middleware bypass, cache poisoning). Currently pinned `^15.5.3`. | Medium-High | Planner to scope a Next.js upgrade — this is a version-bump architectural decision, not a drop-in fix; test the full app after upgrading given how much relies on App Router/middleware behavior. |
| TD-05 | Medium | No CSRF protection on any state-changing route. `lib/middleware/csrf.ts` existed but was deleted (2026-07-16) — it validated a `csrf-token` cookie that nothing in the app ever set, so wiring it in as-is would have broken every mutation. NextAuth's session cookie is a real cookie (not a bearer token), so mutation routes do carry CSRF risk in principle. | Medium | Scope a proper token-issuance flow (e.g. double-submit cookie set at login) if this is judged worth building; otherwise document why it's accepted as-is (e.g. `SameSite=Lax` on the session cookie may already mitigate the common case — verify before deciding). |
| TD-06 | Medium | `lib/middleware/rate-limit.ts` uses a module-level in-memory `Map`, so limits are per-instance, not global. Weak/ineffective if deployed across multiple serverless instances. Second weakness folded in 2026-07-17 (AUD-09): the limiter keys on the raw `x-forwarded-for` header, which is fully client-controlled when the app isn't behind a trusted proxy — rotating the header bypasses every limit (including register's 5/hour), and all unset callers share one `"unknown"` bucket. Acceptable for the current non-deployed, single-instance posture. | Medium | Move to a shared store (e.g. Redis/Upstash) if deployed multi-instance; when doing so, also derive the client IP from the platform's trusted source (e.g. Vercel's `x-real-ip`/first XFF hop set by the edge) rather than the raw header. Otherwise document both as accepted limitations for a single-instance deployment target. |
| TD-07 | Medium | Zero test coverage across ~30 API routes and 8 services prior to 2026-07-16, including the financial logic that is the core of the product (`intrinsic-value.service.ts`, `technical-analysis.service.ts`, `fundamental-analysis.service.ts`) and the buy/sell/closed-position mutation paths. Test runner now exists (`npm run test`) but only covers code touched in the 2026-07-16 onboarding-fix session. | High | Planner to scope a backfill, prioritising the financial scoring services and buy/sell/closed-position paths — miscalculation there is silent and directly wrong for the user. |
| TD-13 | Low | `IndustryComparison` table is read by `intrinsic-value.service.ts` for peer-relative valuation but nothing populates it — those methods always fall back to hardcoded defaults (industry P/E 15, P/B 1.5). | Medium | Either build a job to populate it, or remove the dead read path and document the hardcoded defaults as the real behavior. |
| TD-14 | Low | `ComponentErrorBoundary` only wraps sections on `dashboard/page.tsx` and `portfolio/[ticker]/page.tsx`. `research/[symbol]/page.tsx`, `research/page.tsx`, `wishlist/page.tsx`, `portfolio/add/page.tsx`, `portfolio/closed-positions/page.tsx` (and, as of `plans/2026-07-20-configurable-scoring-weights.md`, the new `settings/page.tsx`) have no boundary around components that depend on external API calls. The full-page `ErrorBoundary` class is defined but never used anywhere, including `app/layout.tsx`. | Low-Medium | Wrap the remaining pages' external-data-dependent sections in `ComponentErrorBoundary`; consider using `ErrorBoundary` at the root layout as a last-resort catch-all. |
| TD-19 | Low | Magic numbers scattered in technical/fundamental scoring (e.g. RSI 30/70 thresholds at `lib/services/technical-analysis.service.ts:408-428`, indicator weights) rather than named constants. Reverified against HEAD 2026-07-16. | Low | Extract to `lib/constants/` when next touching the relevant scoring logic. |
| TD-31 | Low | "Watchlist" (Meridian) vs `wishlist` (code/schema/route) naming split, accepted per ADR-9 for this re-skin. The route (`/wishlist`), `lib/services/wishlist.service.ts`, `app/api/wishlist/**`, and the Prisma `Wishlist`/`WishlistItem` models still say `wishlist`; only user-visible copy says "Watchlist". | Medium | Scope a dedicated rename pass (route + Prisma migration) once a safe, isolated dev database exists (see TD-02) — renaming Prisma models against the shared dev/prod database (ADR-6) is not safe to do casually. **TD-02 was deferred to launch on 2026-07-23, so this item inherits that trigger** — it is blocked until then rather than merely unscheduled. |
| TD-32 | Low | **Narrowed further 2026-07-19** (`plans/2026-07-19-positions-tab.md`, ADR-18): the position-detail Positions tab (renamed from "Transactions") now renders the shared `components/research/transactions-tab.tsx` (`TransactionsTab`) instead of the legacy `TransactionHistory`, which is deleted — the second area this item tracked is resolved. Remaining scope, unchanged from 2026-07-18: `SellPositionModal`/`BuyMoreModal` still use shadcn `Dialog`/`Form`/`Button`/`Input`, off-Meridian. | Low | Reskin `SellPositionModal`/`BuyMoreModal` to the Meridian modal pattern (DESIGN.md "Order-summary total row" governs the Buy-more/Sell-position modals' shared form surface) in a follow-up pass. |
| TD-DTL-SECTOR | Low | Research-detail company header kicker omits the "· sector" clause — `/api/market/quote` does not return a sector field. | Low | Extend the quote service/route to include sector (Yahoo Finance's `assetProfile.sector` module) if a future pass needs it; until then the kicker correctly renders `TICKER · EXCHANGE` only. |
| TD-DTL-SR | Low | Technical tab's `DetailPriceChart` supports dashed support/resistance reference lines + legend as props, but `technical-analysis.service.ts` does not compute support/resistance levels — the chart renders with no reference lines today (by design, per DESIGN.md: never fabricate a level). **Rendering cleanup done 2026-07-18 (MRD-S2, `reviews/2026-07-18-meridian-research-detail.md`):** the shared y-domain denominator was renamed `hoverValueRange` → `valueRange`, and reference-line values are now clamped to `[min, max]` before positioning, so a future out-of-range level renders at the plot edge instead of clipping off the SVG. The underlying data gap (no computed levels) is unchanged. | Medium | Add support/resistance calculation to the technical-analysis service (e.g. pivot points or recent swing highs/lows) and pass the values into `DetailPriceChart`'s `referenceLines` prop. |
| TD-DTL-PEER | Low | Fundamental tab's grading dots (`metricGrade()`) are threshold-based, not peer-relative, because `IndustryComparison` is unpopulated (pre-existing gap, see the `IndustryComparison` fragile-surface entry in `AGENT.md`). The legend still reads "Strong vs peers / In line / Weak vs peers" per the plan's Assumptions. | Low | Reword the legend once a Designer decision is made, or populate `IndustryComparison` and make the dots literally peer-relative (larger effort, tracked separately). |
| TD-DTL-SEG | Low | Fundamental tab's "Revenue by segment" card always renders the empty state ("Segment breakdown not available") — no segment-level revenue data exists in the fundamentals API/service. | Medium | Add segment revenue extraction to `fundamental-analysis.service.ts` (Yahoo Finance does not reliably expose this; may require a different data source) before wiring the bar-list UI. |
| TD-DTL-TXTYPE | Low | Transactions tab's Outlined type badge only distinguishes BUY (green) vs "other" (muted) — the design shows a DIVIDEND transaction type, but the Prisma `Transaction.type` schema only models `BUY`/`SELL`. | Low | If dividend tracking is ever added to the data model, extend the badge's variant logic; no UI change needed until the schema supports a third type. |
| TD-DTL-TONE | Low | News & sentiment tab's 3-col tone band shows current-period % only — the MoM-delta caption line (e.g. "+9 pts vs prior month") is omitted. `SentimentHistory` exists but computing a clean prior-30-day baseline delta is non-trivial and was out of scope for this re-skin pass. | Medium | Add a MoM-delta computation (current 30-day tone % vs the prior 30-day window from `SentimentHistory`) as a small derived value in `news-feed.tsx` or a new helper, then render the caption line per DESIGN.md's tone-band spec. |
| TD-38 | Low | No component/render test seam — `vitest.config.ts` is `environment: "node"`, `include` is `**/*.test.ts` only, no jsdom/happy-dom, `@testing-library/react` not installed, `@vitejs/plugin-react` present in `package.json` but unwired. UI render logic (loading/error/resolved branches, conditional tab visibility, prop plumbing) is verifiable only manually or via extracted pure modules — see ADR-22's `scoring-weights-settings-gate.ts` workaround and `plans/2026-07-23-td08-td15-cleanup.md`'s TD-15 testing note. | Medium | Planner to scope: add jsdom (or happy-dom) + `@testing-library/react`, wire `@vitejs/plugin-react` into `vitest.config.ts`, extend `include` to `*.test.tsx`, and decide whether node and browser tests run as one suite or a vitest `projects` split. Repo-wide convention change — not a drop-in. |
| TD-36 | Low | `border-line` is a dead/no-op Tailwind class (no `--line` CSS variable, no `line` entry in `tailwind.config.js`'s color map — confirmed by `tailwind.config.js:61` only defining `line2`) app-wide, not just on the two bands fixed by `plans/2026-07-20-small-visual-fixes.md` (Issue 1: `intrinsic-value.tsx`, `news-feed.tsx`). It still appears, drawing its rule entirely off `border-t`/`border-b`/`border`'s default color rather than the token name, in: `components/analyst-ratings.tsx:151` (price-target band), `components/technical-analysis.tsx:198` (table header), `components/fundamental-analysis.tsx:111,141`, `components/research/transactions-tab.tsx:161,174,215`, `app/(dashboard)/portfolio/[ticker]/page.tsx`. **Widened 2026-07-21** (SP-S1, `reviews/2026-07-21-scoring-style-presets.md`): `plans/2026-07-21-scoring-style-presets.md`'s Style preset picker (`app/(dashboard)/settings/page.tsx:235`) matches its sibling Reset button (`page.tsx:262`, both pre-existing `border-line` usage) — no new inconsistency introduced, but DESIGN.md's new "Style preset picker" spec repeatedly cites `--line` as if it were a real themed token, reinforcing that a repo-wide decision is now overdue rather than purely cosmetic debt. | Low | Code-hygiene cleanup: replace each `border-line` with `border-line2` (the real hairline token) if a rule is genuinely wanted there, or drop the border utility entirely if not — per band, not a blanket find/replace, since the correct outcome differs per surface. **Open design question, not decided here** (per DESIGN.md "Headline score card" note): should these remaining bands also lose their top rule for system-wide visual convergence with the two bands Issue 1 already cleaned up, or is a rule intentional on tables/price-target bands specifically? Route to the Designer before touching any of these five sites — now also covering the Settings scoring-weights section's picker + Reset button. Once decided, also correct DESIGN.md's `--line` references to match whatever `border-line`/`border-line2` resolves to. |

## Resolved

IDs below are `TD-*` in this table's own namespace; the "resolved by" column cites the originating review finding in `reviews/2026-07-16-onboarding.md` for traceability.

| ID | Description | Resolved by | Resolved |
|----|-------------|-------------|----------|
| TD-20 | Deleted unauthenticated `app/api/test-yahoo` debug route that echoed upstream error text. | ONB-02 | 2026-07-16 |
| TD-21 | Deleted dormant, unwired `lib/middleware/csrf.ts` (see TD-05 for the real follow-up). Wired `lib/middleware/rate-limit.ts` into `/api/auth/register` and the third-party-fanout routes (`market/search`, `news/[symbol]`, `sentiment/[symbol]/history`, `research/[symbol]/intrinsic-value`). | ONB-03 | 2026-07-16 |
| TD-22 | Added auth guard + `days` param clamping to `app/api/sentiment/[symbol]/history/route.ts`. | ONB-04 | 2026-07-16 |
| TD-23 | Added auth guard to `research/[symbol]/intrinsic-value`, `news/[symbol]`, `market/search` routes. | ONB-05 | 2026-07-16 |
| TD-24 | Set up `vitest`, `tsc --noEmit` typecheck script, `gitleaks` secret scan, and a single `npm run verify` command. | ONB-06 | 2026-07-16 |
| TD-25 | Added tests for code touched in the 2026-07-16 session (rate-limit unit tests, auth-guard tests for the 4 newly-protected routes). Full retrofit intentionally out of scope — see TD-07. | ONB-07 | 2026-07-16 |
| TD-26 | Untracked `.next/` from git (`git rm -r --cached .next`); `.gitignore` entry was already correct. | ONB-08 | 2026-07-16 |
| TD-27 | Created `PRODUCT.md`, `ARCHITECTURE.md`, `DECISIONS.md`, `AGENT.md`, `TECH_DEBT.md` from codebase audit; created `GTM.md`, `DESIGN.md`, `future_ideas.md` as `[REQUIRES INPUT]` stubs. | ONB-09 | 2026-07-16 |
| TD-29 | `.env.local` carried the **pre-rotation** values of `NEXTAUTH_SECRET` and `GEMINI_API_KEY`, so the 2026-07-17 rotation was not in effect locally (Next.js gives `.env.local` precedence). Resolved: owner synced `.env.local` to the rotated `.env` values — verified 2026-07-17 by hashing every key in both files against `3855042e:.env.local` (all rotated keys differ from the leaked values; only the ADR-7-accepted `NEWS_API_KEY` matches). This entry was briefly numbered TD-07, colliding with the test-coverage item — renumbered here. | ONB-01 (follow-up) | 2026-07-17 |
| TD-10 | `lib/services/wishlist.service.ts` computed sentiment/intrinsic-value sub-scores via self-referential HTTP `fetch` calls to its own now-authenticated API routes, which always 401'd (silently forcing those sub-scores to `null`/neutral-5). Refactored to call `newsService.getAnalyzedNewsForSymbol()` / `IntrinsicValueService.calculateIntrinsicValue()` directly; also fixed `(score \|\| 5)` → `(score ?? 5)` so a legitimate 0 score isn't treated as missing. | AUD-05 | 2026-07-17 |
| TD-04 | `axios` (CVEs — SSRF, prototype pollution, header injection) removed. Its one call site, `news.service.ts`'s NewsAPI fetch, was rewritten with native `fetch` + `AbortController` (matching `exchange-rate.service.ts`'s existing pattern); dependency removed from `package.json`. | Plan Task 8, `plans/2026-07-18-performance-audit-remediation.md` | 2026-07-18 |
| TD-16 | `zustand` confirmed still unused (zero imports) and removed from `package.json`. | Plan Task 8 | 2026-07-18 |
| TD-30 | `@tremor/react` confirmed still unused (zero imports) and removed from `package.json`. `recharts` also removed — ADR-11 deleted its last caller (`price-chart.tsx`) and this pass confirmed zero remaining imports. | Plan Task 8 | 2026-07-18 |
| TD-35 | `GET /api/portfolio/positions/[ticker]` hardcoded `realizedPL: 0` with a comment claiming the field wasn't implemented in the schema — a pre-existing bug predating the positions-tab feature (`Position` genuinely has no persisted per-position `realizedPL` column; only `Portfolio.realizedPL`, a portfolio-wide accumulator, exists). The old `/portfolio/[ticker]` header's `hasRealizedPL` check was therefore always false in practice. Fixed: the route now computes real per-position realized P/L on read via `computePositionRealizedPL` in `lib/services/realized-pl.service.ts`, reusing the same FIFO algorithm `GET /api/portfolio/closed-positions` already uses per position. | PT-I1, `reviews/2026-07-19-positions-tab.md` (owner decision PT-Q1) | 2026-07-19 |
| TD-DTL-TGT | Analysts tab's price-target Low/High em-dash placeholders resolved: `analyst-ratings.service.ts` now extracts `targetLowPrice`/`targetHighPrice` from the already-fetched `financialData` module and returns them on the API response; `analyst-ratings.tsx` renders real figures when present, em-dash only when Yahoo genuinely returns a mean-only target. Non-persisted on a 24h cache hit — tracked as TD-DTL-REV2. | Plan Task 2, `plans/2026-07-19-research-tab-fixes.md` | 2026-07-20 |
| TD-DTL-REV | Analysts tab's "Recent revisions" empty state resolved: `analyst-ratings.service.ts` now extracts `upgradeDowngradeHistory.history` into a typed `revisions` array (firm, action, fromGrade, toGrade, date) and returns it; `analyst-ratings.tsx` renders a RAISED/HELD/LOWERED table when populated, a reworded earned empty state ("No analyst revisions in the last 90 days.") when genuinely empty. Non-persisted on a 24h cache hit — tracked as TD-DTL-REV2. | Plan Task 3 (OD-3), `plans/2026-07-19-research-tab-fixes.md` | 2026-07-20 |
| TD-DTL-SCEN | Intrinsic value tab's Bear/Bull em-dash placeholders resolved via OD-2's method-spread default (not a flex-assumptions model): `intrinsic-value.service.ts` now returns `scenarioLow`/`scenarioHigh` (min/max of the already-computed valid method values) alongside the existing weighted-average `intrinsicValue`; `intrinsic-value.tsx` renders them as Bear/Bull with honest "Lowest/Highest of N methods" captions, em-dash only when fewer than 2 methods are valid. | Plan Task 6 (OD-2), `plans/2026-07-19-research-tab-fixes.md` | 2026-07-20 |
| TD-DTL-ASSUMP | Intrinsic value tab's "Model assumptions" FCF-margin/terminal-growth em-dash placeholders resolved via OD-1's honest relabel (not a new FCF-perpetuity DCF): the two rows describing inputs the DCF Lite method never computes are dropped, not em-dashed; the assumption list is now the three real DCF Lite inputs — Earnings growth (capped), Terminal P/E, Discount rate (WACC) — each pulled from `dcfLite.inputs`. Also fixed the pre-existing "Revenue growth" mislabel (the row was showing earnings growth). | Plan Tasks 4/5 (OD-1), `plans/2026-07-19-research-tab-fixes.md` | 2026-07-20 |
| TD-DTL-REV2 | Resolved: NVDA follow-on bug root-caused two issues masked by TD-DTL-REV/TD-DTL-TGT — (1) `formatCachedData` hardcoded `revisions: []`/`targetLowPrice`/`targetHighPrice: null` on every 24h cache hit (no DB columns existed), so a heavily-covered, cache-warm symbol like NVDA always rendered the empty state even though a fresh fetch genuinely had 982 upstream revisions (32 within 90 days); (2) there was no 90-day window filter anywhere, so a cold fetch would have rendered all 982 rows mislabelled as "last 90 days". Fixed via an additive migration (OD-1 = Option A, owner-signed-off, mirroring ADR-14): `AnalystRating` gained `targetLowPrice`/`targetHighPrice` (`Decimal?`) and `revisions` (`Json?`) columns, populated in `saveToDatabase` and read back in `formatCachedData`; a new pure `filterRecentRevisions` helper windows/sorts/caps `extractRevisions`' output to the last 90 days (inclusive boundary), newest-first, capped to 25. **Migration created but not yet applied** — `prisma/migrations/20260719231015_analyst_revisions_low_high/migration.sql` is a single additive `ALTER TABLE ADD COLUMN` statement, held for owner `prisma migrate deploy` sign-off per ADR-6/ADR-14 protocol (shared dev/prod DB). | Plan Tasks 1/2, `plans/2026-07-20-analyst-revisions-nvda-fix.md`; ADR-19 | 2026-07-20 |
| TD-09 | `lib/env.ts` and `lib/utils/env.ts` were both dead code (zero importers, both validated `GOOGLE_API_KEY` instead of the real `GEMINI_API_KEY`) — deleted, no replacement built. The centralized-validation gap this leaves is tracked as new TD-37. | `plans/2026-07-23-lib-cleanup-batch.md` (Task 1) | 2026-07-23 |
| TD-11 | Replaced the hardcoded `latestMigrationDate` date-literal cache-freshness gate with an exported `SCORING_VERSION` integer constant (`lib/services/fundamental-analysis.service.ts`), persisted per-row inside the existing `scoreDetails` JSON column (no schema migration). A cached row is fresh only when `scoreDetails.scoringVersion` is greater than or equal to the current `SCORING_VERSION` (`>=`) **and** `lastUpdated` is within 24h — the two gates stay independent. | `plans/2026-07-23-lib-cleanup-batch.md` (Task 5); ADR-27 | 2026-07-23 |
| TD-12 | Consolidated the two independent `new GoogleGenerativeAI(...)` client instantiations (`sentiment.service.ts`, `app/api/insights/portfolio/route.ts`) into a single shared factory, `createGeminiClient`/`getGeminiApiKey` (`lib/services/gemini.ts`). Each call site's differing missing-key behavior (throw-at-construction vs. graceful-placeholder-without-constructing) is unchanged — only the client construction itself is now centralized. `new GoogleGenerativeAI` appears in exactly one place. | `plans/2026-07-23-lib-cleanup-batch.md` (Tasks 2/3); ADR-27 | 2026-07-23 |
| TD-34 | Both options applied: (a) `lib/yahoo-finance.ts`'s shared client now passes `validation: { logErrors: false }`, suppressing only `yahoo-finance2`'s pre-throw console dump — validation stays on, `safeQuoteSummary`'s catch/coerce/warn contract (ADR-15) is unaffected. (b) Dropped the never-consumed `'earningsHistory'` module from the fundamentals `quoteSummary` modules array, removing the specific drift source at the fetch source. | `plans/2026-07-23-lib-cleanup-batch.md` (Tasks 4/6); ADR-27 | 2026-07-23 |
| TD-08 | The last 6 route handlers inlining `getServerSession(authOptions)` + a manual 401 (`market/chart/[symbol]`, `market/quote/[symbol]`, `market/search`, `news/[symbol]`, `research/[symbol]/intrinsic-value`, `sentiment/[symbol]/history`) now call the shared `getAuthenticatedUser()` from `lib/utils/auth.ts:36`. Guard position and the 4 rate-limited routes' guard-before-`checkRateLimit` order are unchanged; none of the 6 binds `auth.userId`. `getServerSession` now appears in zero `app/api/**/route.ts` files. Verified behavior-preserving by `app/api/auth-guard.test.ts` passing with a zero-line diff; 2 previously-uncovered routes (`market/quote`, `market/chart`) gained 401+200 coverage in a new file. | `plans/2026-07-23-td08-td15-cleanup.md` (Tasks 1-3); ADR-28 | 2026-07-23 |
| TD-15 | `app/(dashboard)/research/[symbol]/page.tsx`'s quote fetch moved from `useEffect`/`fetchStockData`/`useState` to `useQuery({ queryKey: ["quote", symbol], ... })`, matching the two `useQuery` calls already in the file and the identical query on `portfolio/[ticker]/page.tsx` (key deliberately shared; `refetchInterval` deliberately not copied). The page now renders three distinct states — `isPending` (existing spinner), `isError` (new: back-link + header + the app's existing full-card failure treatment), resolved (existing body) — fixing the previously-silent swallowed-error path where a failed/non-ok quote fetch rendered identically to a successful-but-empty fetch. | `plans/2026-07-23-td08-td15-cleanup.md` (Tasks 4-6); ADR-28 | 2026-07-23 |
| TD-33 | Closed the remaining hover-marker padding-omission gap: `lib/utils/chart-path.ts` now exports `plotYFraction(value, domainMin, domainMax, height, padding = 8)` — the single padded-domain value→y mapping, returned as a fraction of height — and `gridlineYs` is refactored into a thin wrapper over it. Both charts' hover markers (`portfolio-chart.tsx`'s `hoverYFrac`, `detail-price-chart.tsx`'s `hoverYFrac`) and `detail-price-chart.tsx`'s reference-line `y` now call `plotYFraction` directly instead of the old un-padded `1 - (v - min) / range` formula, so the marker, reference line, gridlines, and the plotted line all read one mapping. | `plans/2026-07-23-td33-hover-marker-padded-domain.md`; ADR-29 | 2026-07-23 |
